const follow = checkFollow();
const alert = checkAlert();

// key 값과 변수명이 같은 경우 생략 가능!
const settings = {
    notifications: {
        flollow, //follow = follow
        alert //alert = alert
    }
};